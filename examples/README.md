# Examples

The sample files in this folder contain synthetic, non-confidential data and are safe for testing.

## Customer contacts sample

Use `customer_contacts_sample.csv` with the **Customer Contacts** preset. The sample intentionally contains inconsistent spacing, an uppercase email, and a duplicate row so the cleaning and quality workflows have something to detect.

Never upload or commit real customer data to this repository.
